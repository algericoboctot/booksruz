export type typeImagePath = string | undefined | boolean;
export type typeImageTitle = string | undefined | boolean;
export type Genres = string[];
export type RESULT_SIZE = number;

export type PageProps = {
    params: {
      genre: string;
    }
}